# 安全说明

## 支持范围

当前售卖包中的最新版本接受安全修复。购买者应保留原始订单信息，并通过购买平台的私密渠道提交漏洞报告。报告应包含受影响版本、复现条件、请求样例和影响说明；在修复可用前请勿公开可利用细节或真实凭据。

## 已实施的安全边界

- 首次安装使用 CSRF、输入校验、并发锁和原子配置写入，`install.lock` 最后生成。
- 后台使用 Session 登录、失败限次、密码哈希、登录后 Session ID 轮换和 CSRF。
- Cookie 默认启用 `HttpOnly` 和 `SameSite=Lax`；部署 HTTPS 后必须设置 `COOKIE.SECURE=true`。
- 本地安装包校验扩展名、真实 MIME、可配置且有安全边界的大小上限和 ZIP 文件签名，保存路径使用随机文件名。
- PNG、JPG、WebP 校验真实 MIME、尺寸与像素上限；SVG 通过 DOM 白名单重新构造。
- 网盘下载仅输出已校验的 HTTP(S) 分享链接与可选密码，由浏览器跳转，不在服务端解析或代理第三方网盘。
- 客户端 IP 仅在请求来自后台配置的可信代理或 CIDR 时读取转发头。
- 数据库查询使用查询构造器或预处理语句，日志筛选与内容字段均有长度和格式边界。
- `public/uploads/.htaccess` 以及部署模板共同禁止上传目录执行脚本。

## 部署方责任

- Web 根目录只能指向 `public`，不得暴露项目根目录。
- `.env`、数据库备份、管理员密码和网盘访问密码均按秘密管理，不进入代码仓库或公开工单。
- 生产环境关闭调试模式，启用 HTTPS、系统安全更新、防火墙和最小权限数据库账号。
- Nginx 部署必须保留上传脚本拦截规则；Apache 部署必须允许 `public/uploads/.htaccess` 生效。
- 定期备份数据库与上传文件，并实际演练恢复流程。
- 每日执行 `php think nova:logs:cleanup --days=90`，保留天数按隐私政策调整。

## ThinkPHP 安全公告边界

`composer audit` 当前会报告 `topthink/framework` 的 [CVE-2024-44902 / GHSA-f4wh-359g-4pq7](https://github.com/advisories/GHSA-f4wh-359g-4pq7)，公告影响范围为 6.1.3 至 8.0.4。公告所述利用前提包括应用主动对不可信数据调用 `unserialize`，并存在 Memcached 扩展环境。

本项目未对请求输入调用 `unserialize`，缓存驱动固定为文件，且不依赖 Memcached，因此当前代码没有满足该公告描述的利用链。该判断不是对上游缺陷的忽略：部署方仍应避免安装 Memcached 扩展或切换到相关缓存驱动，禁止新增不可信反序列化，并在上游发布兼容修复后优先升级和重新测试。

残余风险包括未来业务代码改变利用前提、上游公告信息更新，以及第三方依赖出现新的漏洞。每次发布前都应运行 `composer audit` 并人工评估，不应通过忽略列表让扫描静默通过。

## 事件响应

发现疑似入侵时立即将站点切换为维护状态，保留 Web、PHP、系统和数据库审计日志，轮换 `.env` 中涉及的数据库凭据和管理员密码，并从可信备份恢复。不要先清理日志或覆盖现场；确认影响范围后再恢复服务。
